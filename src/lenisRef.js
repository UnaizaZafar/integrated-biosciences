/**
 * Module-level ref for the Lenis instance. Set by useLenis so other code
 * (e.g. HorizontalScroll) can read the current scroll position when using smooth scroll.
 */
export const lenisRef = { current: null }
